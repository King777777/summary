# 搜索树

```js
const treeData = [
  {
    title: '主合同',
    key: '1',
    children: [
      {
        title: '一般贷款合同',
        key: '1-1',
        children: []
      },
      {
        title: '银团贷款合同',
        key: '1-2',
        children: []
      },
      {
        title: '银行承兑汇票贴现协议',
        key: '1-3',
        children: []
      },
      {
        title: '商业承兑汇票贴现协议',
        key: '1-4',
        children: []
      }
    ]
  },
  {
    title: '从合同',
    key: '2',
    children: [
      {
        title: '保证合同',
        key: '2-1',
        children: [
          {
            title: '一般保证合同',
            key: '2-1-1',
            children: []
          },
          {
            title: '最高额保证合同',
            key: '2-1-1',
            children: []
          }
        ]
      },
      {
        title: '抵押合同',
        key: '2-2',
        children: [
          {
            title: '一般抵押合同',
            key: '2-2-1',
            children: []
          },
          {
            title: '最高额保抵押证合同',
            key: '2-2-2',
            children: []
          }
        ]
      },
      {
        title: '质押合同',
        key: '2-3',
        children: [
          {
            title: '一般质押合同',
            key: '2-3-1',
            children: []
          },
          {
            title: '最高额质押合同',
            key: '2-3-2',
            children: []
          }
        ]
      }

    ]
  },
  {
    title: '其他协议',
    key: '3',
    children: []
  }
]
```

```js
// 递归处理数据，如果需要的话
doRecursion = data => {
  data.forEach(x => {
    x.tagLevel = x.tagName + "_" + x.tagLevel;
    if (x.tags.length > 0) {
      this.doRecursion(x.tags);
    }
  });
};
```

~~~js
this.state = {
    expandedKeys: [],
    searchValue: "",
    autoExpandParent: true,
   selectedTitle: undefined
}
~~~



~~~js
generateLeftContent = () => {
    const {expandedKeys, autoExpandParent} = this.state;
    return (
      <>
        <p style={{fontWeight: 'bold'}}>合同类型</p>
        <Input.Search
          style={{ marginBottom: 8 }}
          placeholder="Search"
          onChange={this.onChange}
        />
        <Tree
          onExpand={this.onExpand}
          expandedKeys={expandedKeys}
          autoExpandParent={autoExpandParent}
          onSelect={this.onSelect}
        >
          {this.renderTreeNodes(treeData)}
        </Tree>
      </>
    );
  }
~~~

Search 的onchange

```js
//targetKeys是个全局变量  
onChange = e => {
    const { value } = e.target;
    this.setState({ inputValue: value });
    targetKeys = [];
    // 递归查找， 招到目标key，然后通过设置autoExpanParent为true自动展开父级
    this.recursionFind(treeData, value);
    this.setState({
      expandedKeys: targetKeys,
      autoExpandParent: true
    });
  };
```

```js

recursionFind = (data, target) => {
    data.forEach(x => {
      if (x.children.length > 0) {
        this.recursionFind(x.children, target);
      }
      if (target && x.title.indexOf(target) > -1) {
        targetKeys.push(x.key);
      }
    });
  };
```

OnExpand

```js
onExpand = expandedKeys => {
  this.setState({
    expandedKeys,
    autoExpandParent: false
  });
};
```

onSelect选择时调查询

```js
onSelect = selectedKey => {
  console.log("selectedKey", selectedKey);
  if (selectedKey.length === 0) {
    this.setState({ selectedTagName: undefined });
    return;
  }
  const selectedString = selectedKey[0];
  const splitIndex = selectedString.indexOf("_");
  const tagName = selectedString.substring(0, splitIndex);
  this.setState({ pageNum: 1, selectedTagName: tagName }, this.getCompData);
};
```

渲染树

```js
renderTreeNodes = data => {
    const {inputValue} = this.state;
    if (data && data.length > 0) {
      return data.map(item => {
        if (item.children && item.children.length > 0) {
          return (
            <Tree.TreeNode title={<>{item.title}</>} key={item.key}>
              {this.renderTreeNodes(item.children)}
            </Tree.TreeNode>
          );
        }
        const index = item.title.indexOf(inputValue);
        const beforeStr = item.title.substr(0, index);
        const afterStr = item.title.substr(index + inputValue?.length);
        const title =
          index > -1 ? (
            <span>
              {beforeStr}
              <span style={{ color: '#f50' }}>{inputValue}</span>
              {afterStr}
            </span>
          ) : (
            <span>{item.title}</span>
          );
        return (
          <Tree.TreeNode
            title={title}
            key={item.key}
          />
        );
      });
    }
    return "";
  };
```

DEMo

~~~js
import React, { Component } from 'react';
import { Card, Divider, Form, Row, Col, Button, Radio, Input, Checkbox, Tree } from 'antd';
import { YRIndexPageLayout, YRLeftRightLayout } from 'yrantd';
import NormLoanContract from '@/pages/Contract/KindManagement/Cards/NormLoanContract';

const treeData = [
  {
    title: '主合同',
    key: '1',
    children: [
      {
        title: '一般贷款合同',
        key: '1-1',
        children: []
      },
      {
        title: '银团贷款合同',
        key: '1-2',
        children: []
      },
      {
        title: '银行承兑汇票贴现协议',
        key: '1-3',
        children: []
      },
      {
        title: '商业承兑汇票贴现协议',
        key: '1-4',
        children: []
      }
    ]
  },
  {
    title: '从合同',
    key: '2',
    children: [
      {
        title: '保证合同',
        key: '2-1',
        children: [
          {
            title: '一般保证合同',
            key: '2-1-1',
            children: []
          },
          {
            title: '最高额保证合同',
            key: '2-1-1',
            children: []
          }
        ]
      },
      {
        title: '抵押合同',
        key: '2-2',
        children: [
          {
            title: '一般抵押合同',
            key: '2-2-1',
            children: []
          },
          {
            title: '最高额保抵押证合同',
            key: '2-2-2',
            children: []
          }
        ]
      },
      {
        title: '质押合同',
        key: '2-3',
        children: [
          {
            title: '一般质押合同',
            key: '2-3-1',
            children: []
          },
          {
            title: '最高额质押合同',
            key: '2-3-2',
            children: []
          }
        ]
      }

    ]
  },
  {
    title: '其他协议',
    key: '3',
    children: []
  }
]
const { TreeNode } = Tree;
let targetKeys = [];

class KindManagement extends Component {
  constructor(props) {
    super(props);
    this.state = {
      expandedKeys: [],
      searchValue: "",
      autoExpandParent: true,
      selectedTitle: undefined
    }
  }

  onSelect = (selectedKeys, info) => {
    console.log('selected', selectedKeys, info);
  };

  onCheck = (checkedKeys, info) => {
    console.log('onCheck', checkedKeys, info);
  };

  onChange = e => {
    const { value } = e.target;
    this.setState({ inputValue: value });
    targetKeys = [];
    this.recursionFind(treeData, value);
    this.setState({
      expandedKeys: targetKeys,
      autoExpandParent: true
    });
  };

  recursionFind = (data, target) => {
    data.forEach(x => {
      if (x.children.length > 0) {
        this.recursionFind(x.children, target);
      }
      if (target && x.title.indexOf(target) > -1) {
        targetKeys.push(x.key);
      }
    });
  };

  onExpand = expandedKeys => {
    this.setState({
      expandedKeys,
      autoExpandParent: false
    });
  };

  generateLeftContent = () => {
    const {expandedKeys, autoExpandParent} = this.state;
    return (
      <>
        <p style={{fontWeight: 'bold'}}>合同类型</p>
        <Input.Search
          style={{ marginBottom: 8 }}
          placeholder="Search"
          onChange={this.onChange}
        />
        <Tree
          onExpand={this.onExpand}
          expandedKeys={expandedKeys}
          autoExpandParent={autoExpandParent}
          onSelect={this.onSelect}
        >
          {this.renderTreeNodes(treeData)}
        </Tree>
      </>
    );
  }

  renderTreeNodes = data => {
    const {inputValue} = this.state;
    if (data && data.length > 0) {
      return data.map(item => {
        if (item.children && item.children.length > 0) {
          return (
            <Tree.TreeNode title={<>{item.title}</>} key={item.key}>
              {this.renderTreeNodes(item.children)}
            </Tree.TreeNode>
          );
        }
        const index = item.title.indexOf(inputValue);
        const beforeStr = item.title.substr(0, index);
        const afterStr = item.title.substr(index + inputValue?.length);
        const title =
          index > -1 ? (
            <span>
              {beforeStr}
              <span style={{ color: '#f50' }}>{inputValue}</span>
              {afterStr}
            </span>
          ) : (
            <span>{item.title}</span>
          );
        return (
          <Tree.TreeNode
            title={title}
            key={item.key}
          />
        );
      });
    }
    return "";
  };

  generateRightContent = () => {
    return (
      <NormLoanContract />
    );
  }

  render() {
    return (
      <YRLeftRightLayout leftContent={this.generateLeftContent()} rightContent={this.generateRightContent()} />
    );
  }
}

export default KindManagement;

~~~

