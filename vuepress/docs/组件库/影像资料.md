~~~jsx
/**
 * 影像资料配置
 * @author: liuwq
 * @since: 2021-07-08 15:27:36
 */

import React, { Component } from 'react';
import { Form, Card, Modal, Divider, Tree } from '../Desktop/Learing/学习/antd';
import { queryTree } from '@/services/product';


const FormItem = Form.Item;

class ImageSetCard extends Component {
  state = {
    visible: false,
    treeData: [],
      //mubiao
    targetKeys: [],
  };

TreeTransfer = () => {
    
    
}

  componentDidMount() {
    queryTree().then(res => {
      if (res && res.success) {
        const treeData = res.data;
        this.setState({ treeData });
      }
    });
  }

  showModal = () => {
    this.setState({
      visible: true,
    });
    console.log('弹窗输出');
    console.log(this.state.treeData);
  };

  handleOk = e => {
    console.log(e);
    this.setState({
      visible: false,
    });
  };

  handleCancel = e => {
    console.log(e);
    this.setState({
      visible: false,
    });
  };

onChange = keys => {
    this.setState({targetKeys: keys});
}

  renderTreeNodes = data =>
      data.map(item => {
        if (item.children) {
          return (
              <Tree.TreeNode title={item.categoryName} key={item.category}>
                {this.renderTreeNodes(item.children)}
              </Tree.TreeNode>
          );
        }
        return <Tree.TreeNode {...item} />;
      });

  render() {

    return (
      <Card id="imageSet" title="影像资料配置" hoverable style={{ marginTop: 20 }}>
        <a onClick={this.showModal}>选择影像资料</a>
        <Modal
          title="选择影像资料"
          visible={this.state.visible}
          onOk={this.handleOk}
          onCancel={this.handleCancel}
        >
            <this.TreeTransfer
                datasource={this.state.treeData}
                targetKeys={this.state.targetKeys}
               onChange={this.onChange}
                ></this.TreeTransfer>
         
            
            <Tree checkable>
            {this.renderTreeNodes(this.state.treeData)}
          </Tree>
          <Divider type="vertical" />
          <Tree checkable>
            {this.renderTreeNodes(this.state.treeData)}
          </Tree>
        </Modal>
      </Card>
    );
  }
}
export default ImageSetCard;

~~~

~~~jsx

import React from 'react';
import ReactDOM from 'react-dom';
import 'antd/dist/antd.css';
import './index.css';
import {Tree, Row, Col} from 'vuepress/docs/组件库/antd';

const {TreeNode} = Tree;

const treeData = [
  {
    title: '0-0',
    key: '0-0',
    children: [
      {
        title: '0-0-0',
        key: '0-0-0',
        children: [
          {title: '0-0-0-0', key: '0-0-0-0'},
          {title: '0-0-0-1', key: '0-0-0-1'},
          {title: '0-0-0-2', key: '0-0-0-2'},
        ],
      },
      {
        title: '0-0-1',
        key: '0-0-1',
        children: [
          {title: '0-0-1-0', key: '0-0-1-0'},
          {title: '0-0-1-1', key: '0-0-1-1'},
          {title: '0-0-1-2', key: '0-0-1-2'},
        ],
      },
      {
        title: '0-0-2',
        key: '0-0-2',
      },
    ],
  },
  {
    title: '0-1',
    key: '0-1',
    children: [
      {title: '0-1-0-0', key: '0-1-0-0'},
      {title: '0-1-0-1', key: '0-1-0-1'},
      {title: '0-1-0-2', key: '0-1-0-2'},
    ],
  },
  {
    title: '0-2',
    key: '0-2',
  },
];

const rightTreeData = [
  {
    title: '0-0',
    key: '0-0',
    children: [
      {
        title: '0-0-0',
        key: '0-0-0',
        children: [
          {title: '0-0-0-0', key: '0-0-0-0'},
          {title: '0-0-0-1', key: '0-0-0-1'},
          {title: '0-0-0-2', key: '0-0-0-2'},
        ],
      },
      {
        title: '0-0-1',
        key: '0-0-1',
        children: [
          {title: '0-0-1-0', key: '0-0-1-0'},
          {title: '0-0-1-1', key: '0-0-1-1'},
          {title: '0-0-1-2', key: '0-0-1-2'},
        ],
      },
      {
        title: '0-0-2',
        key: '0-0-2',
      },
    ],
  },
  {
    title: '0-1',
    key: '0-1',
    children: [
      {title: '0-1-0-0', key: '0-1-0-0'},
      {title: '0-1-0-1', key: '0-1-0-1'},
      {title: '0-1-0-2', key: '0-1-0-2'},
    ],
  },
  {
    title: '0-2',
    key: '0-2',
  },
];

class Demo extends React.Component {
  state = {
    checkedKeys: ['0-0-0'],
    rightCheckedKeys: [],
  };

  onCheck = checkedKeys => {
    console.log('onCheck', checkedKeys);
    this.setState({checkedKeys});
  };

  onRightCheck = checkedKeys => {
    console.log('onRightCheck', checkedKeys);
    this.setState({rightCheckedKeys: checkedKeys});
  };


  renderTreeNodes = data =>
    data.map(item => {
      if (item.children) {
        return (
          <TreeNode title={item.title} key={item.key} dataRef={item}>
            {this.renderTreeNodes(item.children)}
          </TreeNode>
        );
      }
      return <TreeNode key={item.key} {...item} />;
    });

  renderRightTreeNodes = data =>
    data.map(item => {
      if (item.children) {
        return (
          <TreeNode title={item.title} key={item.key} dataRef={item}>
            {this.renderTreeNodes(item.children)}
          </TreeNode>
        );
      }
      return <TreeNode key={item.key} {...item} />;
    });

  render() {
    return (
      <Row>
        <Col span={12}>
          <Tree
            checkable
            onCheck={this.onCheck}
            checkedKeys={this.state.checkedKeys}
            selectedKeys={this.state.selectedKeys}
          >
            {this.renderTreeNodes(treeData)}
          </Tree>
        </Col>

        <Col span={12}>
          <Tree
            checkable
            onCheck={this.onCheck}
            checkedKeys={this.state.checkedKeys}
          >
            {this.renderTreeNodes(treeData)}
          </Tree>
        </Col>
      </Row>
    );
  }
}

ReactDOM.render(<Demo/>, document.getElementById('container'));

~~~

2

~~~jsx

import React from 'react';
import ReactDOM from 'react-dom';
import 'antd/dist/antd.css';
import './index.css';
import { Tree, Row, Col } from 'antd';

const { TreeNode } = Tree;

const treeData = [
  {
    title: '0-0',
    key: '0-0',
    children: [
      {
        title: '0-0-0',
        key: '0-0-0',
        children: [
          { title: '0-0-0-0', key: '0-0-0-0' },
          { title: '0-0-0-1', key: '0-0-0-1' },
          { title: '0-0-0-2', key: '0-0-0-2' },
        ],
      },
      {
        title: '0-0-1',
        key: '0-0-1',
        children: [
          { title: '0-0-1-0', key: '0-0-1-0' },
          { title: '0-0-1-1', key: '0-0-1-1' },
          { title: '0-0-1-2', key: '0-0-1-2' },
        ],
      },
      {
        title: '0-0-2',
        key: '0-0-2',
      },
    ],
  },
  {
    title: '0-1',
    key: '0-1',
    children: [
      { title: '0-1-0-0', key: '0-1-0-0' },
      { title: '0-1-0-1', key: '0-1-0-1' },
      { title: '0-1-0-2', key: '0-1-0-2' },
    ],
  },
  {
    title: '0-2',
    key: '0-2',
  },
];

const rightTreeData = [
  {
    title: '0-0',
    key: '0-0',
    show: '1',
    children: [
      {
        title: '0-0-0',
        key: '0-0-0',
        show: '1',
        children: [
          { title: '0-0-0-0', key: '0-0-0-0', show: '1' },
          { title: '0-0-0-1', key: '0-0-0-1', show: '1' },
          { title: '0-0-0-2', key: '0-0-0-2', show: '1' },
        ],
      },
      {
        title: '0-0-1',
        key: '0-0-1',
        show: '1',
        children: [
          { title: '0-0-1-0', key: '0-0-1-0', show: '1' },
          { title: '0-0-1-1', key: '0-0-1-1', show: '1' },
          { title: '0-0-1-2', key: '0-0-1-2', show: '1' },
        ],
      },
      {
        title: '0-0-2',
        key: '0-0-2',
        show: '1',
      },
    ],
  },
  {
    title: '0-1',
    key: '0-1',
    show: '0',
    children: [
      { title: '0-1-0-0', key: '0-1-0-0', show: '0' },
      { title: '0-1-0-1', key: '0-1-0-1', show: '0' },
      { title: '0-1-0-2', key: '0-1-0-2', show: '0' },
    ],
  },
  {
    title: '0-2',
    key: '0-2',
    show: '0'
  },
];

class Demo extends React.Component {
  state = {
    checkedKeys: [],
    rightCheckedKeys: [],
  };

  onCheck = checkedKeys => {
    console.log('onCheck', checkedKeys);
    this.setState((state) => ({
      checkedKeys
    }));
 
  };

  onRightCheck = checkedKeys => {
    console.log('onRightCheck', checkedKeys);
    this.setState((state) => ({
      rightCheckedKeys: checkedKeys
    }));
  
  };


  renderTreeNodes = data =>
    data.map(item => {
      if (item.children) {
        return (
          <TreeNode title={item.title} key={item.key} dataRef={item}>
            {this.renderTreeNodes(item.children)}
          </TreeNode>
        );
      }
      return <TreeNode key={item.key} {...item} />;
    });

    renderRightTreeNodes = data =>
    data.map(item => {
      console.log("item", item);
      if (item.children) {
        return (
          <TreeNode title={item.title} key={item.key} dataRef={item}>
            {this.renderTreeNodes(item.children)}
          </TreeNode>
        );
      }
      return <TreeNode key={item.key} {...item} />;
    });

  render() {
    return (
      <Row>
        <Col span={12}>
          <Tree
            checkable
            onCheck={this.onCheck}
            checkedKeys={this.state.checkedKeys}
         >
            {this.renderTreeNodes(treeData)}
        </Tree>
        </Col>

        <Col span={12}>
          <Tree
            checkable
            onCheck={this.onRightCheck}
            checkedKeys={this.state.rightCheckedKeys}
          >
            {this.renderTreeNodes(rightTreeData)}
        </Tree>
        </Col>
      </Row>
    );
  }
}

ReactDOM.render(<Demo />, document.getElementById('container'));
          
~~~

~~~
单一简易版本
import React from 'react';
import ReactDOM from 'react-dom';
import 'antd/dist/antd.css';
import './index.css';
import { Tree } from 'antd';

const { TreeNode } = Tree;

const treeData =  [
  {
    title: '0-0',
    key: '0-0',
    show: '1',
    children: [
      {
        title: '0-0-0',
        key: '0-0-0',
        show: '1',
        children: [
          { title: '0-0-0-0', key: '0-0-0-0', show: '1' },
          { title: '0-0-0-1', key: '0-0-0-1', show: '0' },
          { title: '0-0-0-2', key: '0-0-0-2', show: '0' },
        ],
      },
      {
        title: '0-0-1',
        key: '0-0-1',
        show: '1',
        children: [
          { title: '0-0-1-0', key: '0-0-1-0', show: '1' },
          { title: '0-0-1-1', key: '0-0-1-1', show: '1' },
          { title: '0-0-1-2', key: '0-0-1-2', show: '1' },
        ],
      },
      {
        title: '0-0-2',
        key: '0-0-2',
        show: '1',
      },
    ],
  },
  {
    title: '0-1',
    key: '0-1',
    show: '0',
    children: [
      { title: '0-1-0-0', key: '0-1-0-0', show: '0' },
      { title: '0-1-0-1', key: '0-1-0-1', show: '0' },
      { title: '0-1-0-2', key: '0-1-0-2', show: '0' },
    ],
  },
  {
    title: '0-2',
    key: '0-2',
    show: '0'
  },
];

class Demo extends React.Component {
  state = {
   
    checkedKeys: ['0-0-0'],
   
  };

  onExpand = expandedKeys => {
    console.log('onExpand', expandedKeys);
    // if not set autoExpandParent to false, if children expanded, parent can not collapse.
    // or, you can remove all expanded children keys.
    this.setState({
      expandedKeys,
      autoExpandParent: false,
    });
  };

  onCheck = checkedKeys => {
    console.log('onCheck', checkedKeys);
    this.setState({ checkedKeys });
  };

  onSelect = (selectedKeys, info) => {
    console.log('onSelect', info);
    this.setState({ selectedKeys });
  };

  renderTreeNodes = data =>
    data.map(item => {
      console.log("item",item);
      if ( item.children) {
        return (
          <TreeNode title={item.title} key={item.key} dataRef={item}>
            {this.renderTreeNodes(item.children)}
          </TreeNode>
        );
      }
      return (item.show==='1' && <TreeNode key={item.key} {...item} />);
    });

  render() {
    return (
      <Tree
        checkable
       
        onCheck={this.onCheck}
        checkedKeys={this.state.checkedKeys}
       
      >
        {this.renderTreeNodes(treeData)}
      </Tree>
    );
  }
}

ReactDOM.render(<Demo />, document.getElementById('container'));
          
~~~





**显示树的关键代码**

~~~jsx
 
//两处判断，即可完成显示，否则会有遗漏
renderTreeNodes = data =>
    data.map(item => {
      console.log("item",item);
      if ( item.children) {
        return (
          item.show === '1' &&
          <TreeNode title={item.title} key={item.key} dataRef={item}>
            {this.renderTreeNodes(item.children)}
          </TreeNode>
        );
      }
      return (item.show==='1' && <TreeNode key={item.key} {...item} />);
    });
~~~

onCheck, 第二个关键参数e，千万不要直接打印，会卡死浏览器，e.halfCheckedKeys包含其半选节点（父节点）

~~~jsx
  onRightCheck = (checkedKeys,e) => {
    console.log('onRightCheck', checkedKeys);
    console.log("e.half",e.halfCheckedKeys);
    this.setState((state) => ({
      rightCheckedKeys: checkedKeys
    }));
  
  };
~~~

data.push(Object.assign({}, item, { lineId: 1 }))

~~~jsx
/**
 * 影像资料配置
 * @author: liuwq
 * @since: 2021-07-08 15:27:36
 */

import React, { Component } from 'react';
import { Form, Card, Modal, Divider, Tree, Transfer, Row, Col } from 'antd';
import { queryTree } from '@/services/product';


const FormItem = Form.Item;


class ImageSetCard extends Component {
  state = {
    visible: false,
    leftTreeData: [],
    rightTreeData: [],
    prevChecked: [], // 记录上一个选中状态
  };

  // 递归向其中插show属性，1展示，0隐藏
  doRecursion = (data) => {
    data.forEach(x => {
      if (x.children) {
        this.doRecursion(x.children);
      }
      x.show = '0';
    });
  }

  doRecursionMinus = (data, item) => {
    data.forEach(x => {
      if (x.children) {
        this.doRecursionMinus(x.children, item);
      }
      x.show = '0';
    });
  }

  recursionChange = (data, item) => {
    data.forEach(x => {
      if (x.children) {
        this.recursionChange(x.children, item);
      }
      if (x.category == item) {
        x.show = '1';
      }
    });
  }

  recursionAdd = (data, item) => {
    data.forEach(x => {
      if (x.children) {
        this.recursionAdd(x.children, item);
      }
      if (x.category == item) {
        x.show = '1';
      }
    });
  }
  recursionMinus = (data, item) => {
    data.forEach(x => {
      if (x.children) {
        this.recursionMinus(x.children, item);
      }
      if (x.category == item) {
        x.show = '0';
      }
    });
  }

  // 修改右树 使现形
  doChangeRight = (item) => {
    // eslint-disable-next-line react/no-access-state-in-setstate
    const data = this.state.rightTreeData;
    this.recursionChange(data, item);
    this.setState({ rightTreeData: data });
  }

  // 增加右树 使现形
  doAddRight = (item) => {
    // eslint-disable-next-line react/no-access-state-in-setstate
    const data = this.state.rightTreeData;
    this.recursionAdd(data, item);
    this.setState({ rightTreeData: data });
  }
  // 裁减右树 使隐藏
  doMinusRight = (item) => {
    // eslint-disable-next-line react/no-access-state-in-setstate
    const data = this.state.rightTreeData;
    this.recursionMinus(data, item);
    this.setState({ rightTreeData: data });
  }


  // 初始化左右树，遍历右树给每一个节点加一个show变量，show为‘1’展示，为‘0‘不展示
  componentDidMount() {
    queryTree().then(res => {
      if (res && res.success) {
        const leftTreeData = res.data;
        this.setState({ leftTreeData });

        const tempData = leftTreeData;
        this.doRecursion(tempData);


        this.setState({ rightTreeData: tempData });
        console.log('after recursion,rightData', this.state.rightTreeData);
      }
    });
  }

  showModal = () => {
    this.setState({
      visible: true,
    });
  };

  handleOk = e => {
    console.log(e);
    this.setState({
      visible: false,
    });
  };

  handleCancel = e => {
    console.log(e);
    this.setState({
      visible: false,
    });
  };

  // 点击左树节点，同时在右树显示或隐藏
  onLeftCheck = (checkedKeys, e) => {
    console.log('onCheck', checkedKeys); // 选中的节点
    console.log('e.halfCheckedKeys', e.halfCheckedKeys); // 半选节点（父节点）

    // const prev = this.state; // 获取上一个状态
    // const addNodes = []; // 加集合
    // const minusNodes = []; // 减集合
     const cur = []; // 当前所有要渲染的节点的key


    // // 获得当前选中所有节点，和原先节点比较决定加或减
    checkedKeys.forEach(item => {
      cur.push(item);
    });
    e.halfCheckedKeys.forEach(item => {
      cur.push(item);
    });

    // for (let i = 0; i < cur.length; i++) {
    //   for (let j = 0; j < prev.length; j++) {
    //     if (cur[i] === prev[j]) {
    //       break;
    //     }
    //     if (j === (prev.length - 1)) {
    //       addNodes.push(cur[i]);
    //     }
    //   }
    // }
    //
    // for (let i = 0; i < prev.length; i++) {
    //   for (let j = 0; j < cur.length; j++) {
    //     if (prev[i] === cur[j]) {
    //       break;
    //     }
    //     if (j === (cur.length - 1)) {
    //       minusNodes.push(prev[i]);
    //     }
    //   }
    // }
    //
    // addNodes.forEach(item => {
    //   this.doAddRight(item);
    // });
    //
    // minusNodes.forEach(item => {
    //   this.doMinusRight(item);
    // });
    //
    // this.setState({prev})

   // console.log('curData', cur);
   cur.forEach(item => {
     this.doChangeRight(item);
   });
  };


  // 左树正常渲染
  renderLeftTreeNodes = data =>
      data.map(item => {
        if (item.children) {
          return (
            <Tree.TreeNode title={item.categoryName} key={item.category}>
              {this.renderLeftTreeNodes(item.children)}
            </Tree.TreeNode>
          );
        }
        return <Tree.TreeNode {...item} />;
      });

  // 渲染右树的方法, 判断各个节点的show变量，从而决定渲染与否
  renderRightTreeNodes = data =>
      data.map(item => {
        if (item.children) {
          return (
              item.show === '1' &&
              <Tree.TreeNode title={item.categoryName} key={item.category} dataRef={item}>
                {this.renderRightTreeNodes(item.children)}
              </Tree.TreeNode>
          );
        }
        return (item.show === '1' && <Tree.TreeNode key={item.key} {...item} />);
      });

  render() {
    // const {
    //   form,
    //   form: { getFieldDecorator },
    //   detail,
    // } = this.props;

    return (
      <Card id="imageSet" title="影像资料配置" hoverable style={{ marginTop: 20 }}>
        <a onClick={this.showModal}>选择影像资料</a>
        <Modal
          title="选择影像资料"
          visible={this.state.visible}
          onOk={this.handleOk}
          onCancel={this.handleCancel}
        >
          <Row>
            <Col span={12}>
              <Tree
                checkable
                onCheck={this.onLeftCheck}
              >
                {this.renderLeftTreeNodes(this.state.leftTreeData)}
              </Tree>
            </Col>

            <Col span={12}>
              <Tree
                checkable
              >
                {this.renderRightTreeNodes(this.state.rightTreeData)}
              </Tree>
            </Col>
          </Row>
        </Modal>
      </Card>);
  }
}
export default ImageSetCard;


~~~

~~~jsx

import React from 'react';
import ReactDOM from 'react-dom';
import 'antd/dist/antd.css';
import './index.css';
import { Tree,Divider } from 'antd';

const { TreeNode } = Tree;
const prevChecked = [];

// 左树
const treeData = [
  {
    title: '0-0',
    key: '0-0',
    children: [
      {
        title: '0-0-0',
        key: '0-0-0',
        children: [
          { title: '0-0-0-0', key: '0-0-0-0' },
          { title: '0-0-0-1', key: '0-0-0-1' },
          { title: '0-0-0-2', key: '0-0-0-2' },
        ],
      },
      {
        title: '0-0-1',
        key: '0-0-1',
        children: [
          { title: '0-0-1-0', key: '0-0-1-0' },
          { title: '0-0-1-1', key: '0-0-1-1' },
          { title: '0-0-1-2', key: '0-0-1-2' },
        ],
      },
      {
        title: '0-0-2',
        key: '0-0-2',
      },
    ],
  },
  {
    title: '0-1',
    key: '0-1',
    children: [
      { title: '0-1-0-0', key: '0-1-0-0' },
      { title: '0-1-0-1', key: '0-1-0-1' },
      { title: '0-1-0-2', key: '0-1-0-2' },
    ],
  },
  {
    title: '0-2',
    key: '0-2',
  },
];


// 右树
const treeRightData = [
  {
    title: '0-0',
    key: '0-0',
    sbow: '0',
    children: [
      {
        title: '0-0-0',
        key: '0-0-0',
        sbow: '0',
        children: [
          { title: '0-0-0-0', key: '0-0-0-0', sbow: '0' },
          { title: '0-0-0-1', key: '0-0-0-1', sbow: '0' },
          { title: '0-0-0-2', key: '0-0-0-2', sbow: '0' },
        ],
      },
      {
        title: '0-0-1',
        key: '0-0-1',
        sbow: '0',
        children: [
          { title: '0-0-1-0', key: '0-0-1-0', sbow: '0' },
          { title: '0-0-1-1', key: '0-0-1-1', sbow: '0' },
          { title: '0-0-1-2', key: '0-0-1-2', sbow: '0' },
        ],
      },
      {
        title: '0-0-2',
        key: '0-0-2',
        sbow: '0',
      },
    ],
  },
  {
    title: '0-1',
    key: '0-1',
    children: [
      { title: '0-1-0-0', key: '0-1-0-0' },
      { title: '0-1-0-1', key: '0-1-0-1' },
      { title: '0-1-0-2', key: '0-1-0-2' },
    ],
  },
  {
    title: '0-2',
    key: '0-2',
  },
];


class Demo extends React.Component {
  state = {
    leftTreeData: [],
    rightTreeData: [],
  };

  doChangeRight = (item) => {
    // eslint-disable-next-line react/no-access-state-in-setstate
    const data = this.state.rightTreeData;
    console.log("do ChangeRight", data);
    console.log("item", item);
    this.recursionChange(data, item);
    const temp = data;
    this.setState({ rightTreeData: temp });
  }

  
  recursionChange = (data, item) => {
    console.log("recursion");
    data.forEach(x => {
      if (x.children) {
        this.recursionChange(x.children, item);
      }
      if (x.key === item) {
        console.log("do show");
        x.show = '1';
      }
    });
  }

  componentDidMount(){
    this.setState({leftTreeData: treeData});
    this.setState({rightTreeData: treeRightData});
    console.log(treeData);
    console.log(treeRightData);
  }

  onExpand = expandedKeys => {
    console.log('onExpand', expandedKeys);
    // if not set autoExpandParent to false, if children expanded, parent can not collapse.
    // or, you can remove all expanded children keys.
    this.setState({
      expandedKeys,
      autoExpandParent: false,
    });
  };

  onCheck = (checkedKeys, e) => {
    console.log('onCheck', checkedKeys);
    console.log('halfCheckedKeys', e.halfCheckedKeys);

  //   const prev = prevChecked;
  //   console.log('prev', prev);
  //   const addNodes = []; // 加集合
  //   const minusNodes = []; // 减集合
     const cur = []; // 当前所有要渲染的节点的key


   // // 获得当前选中所有节点，和原先节点比较决定加或减
   checkedKeys.forEach(item => {
     cur.push(item);
   });
   e.halfCheckedKeys.forEach(item => {
     cur.push(item);
   });

  //  if (prev.length === 0) {
  //    cur.forEach(item => {
  //      addNodes.push(item);
  //    });
  //  } else {
  //    for (let i = 0; i < cur.length; i++) {
  //      for (let j = 0; j < prev.length; j++) {
  //        if (cur[i] === prev[j]) {
  //          break;
  //        }
  //        if (j === (prev.length - 1)) {
  //          addNodes.push(cur[i]);
  //        }
  //      }
  //    }
  //  }

  //  if (cur.length === 0) {
  //    prev.forEach(item => {
  //      minusNodes.push(item);
  //    });
  //  } else {
  //    for (let i = 0; i < prev.length; i++) {
  //      for (let j = 0; j < cur.length; j++) {
  //        if (prev[i] === cur[j]) {
  //          break;
  //        }
  //        if (j === (cur.length - 1)) {
  //          minusNodes.push(prev[i]);
  //        }
  //      }
  //    }
  //  }
  //  cur.forEach(item => {
  //    prevChecked.push(item);
  //  });
  //  console.log('cur -> prev',prevChecked);



  //  console.log('addNodes', addNodes);
  //  console.log('minusNodes', minusNodes);


   // addNodes.forEach(item => {
   //   this.doAddRight(item);
   // });
   //
   // minusNodes.forEach(item => {
   //   this.doMinusRight(item);
   // });

  //  this.setState({ prevChecked: cur });

  console.log('curData', cur);
  cur.forEach(item => {
    this.doChangeRight(item);
  });

  };

  onSelect = (selectedKeys, info) => {
    console.log('onSelect', info);
    this.setState({ selectedKeys });
  };

  renderTreeNodes = data =>
    data.map(item => {
      if (item.children) {
        return (
          <TreeNode title={item.title} key={item.key} dataRef={item}>
            {this.renderTreeNodes(item.children)}
          </TreeNode>
        );
      }
      return <TreeNode key={item.key} {...item} />;
    });

    renderRightTreeNodes = data =>
    data.map(item => {
      if (item.children) {
        return (
          item.show === '1' &&
          <TreeNode title={item.title} key={item.key} dataRef={item}>
            {this.renderTreeNodes(item.children)}
          </TreeNode>
        );
      }
      return item.show === '1' && <TreeNode key={item.key} {...item} />;
    });

  render() {
    return (
      <div>
      <Tree
        checkable     
        onCheck={this.onCheck} 
      >
        {this.renderTreeNodes(this.state.leftTreeData)}
      </Tree>
      <Divider type = "horizontal" />
      <Tree
        checkable     
       // checkedKeys={this.state.checkedKeys}    
      >
        {this.renderRightTreeNodes(this.state.rightTreeData)}
      </Tree>

      </div>
      
    );
  }
}

ReactDOM.render(<Demo />, document.getElementById('container'));
          
~~~





~~~jsx

import React from 'react';
import ReactDOM from 'react-dom';
import 'antd/dist/antd.css';
import './index.css';
import { Tree,Divider, Button } from 'antd';

const { TreeNode } = Tree;
const prevChecked = [];

// 左树
const treeData = [
  {
    title: '0-0',
    key: '0-0',
    children: [
      {
        title: '0-0-0',
        key: '0-0-0',
        children: [
          { title: '0-0-0-0', key: '0-0-0-0' },
          { title: '0-0-0-1', key: '0-0-0-1' },
          { title: '0-0-0-2', key: '0-0-0-2' },
        ],
      },
      {
        title: '0-0-1',
        key: '0-0-1',
        children: [
          { title: '0-0-1-0', key: '0-0-1-0' },
          { title: '0-0-1-1', key: '0-0-1-1' },
          { title: '0-0-1-2', key: '0-0-1-2' },
        ],
      },
      {
        title: '0-0-2',
        key: '0-0-2',
      },
    ],
  },
  {
    title: '0-1',
    key: '0-1',
    children: [
      { title: '0-1-0-0', key: '0-1-0-0' },
      { title: '0-1-0-1', key: '0-1-0-1' },
      { title: '0-1-0-2', key: '0-1-0-2' },
    ],
  },
  {
    title: '0-2',
    key: '0-2',
  },
];


// 右树
const treeRightData = [
  {
    title: '0-0',
    key: '0-0',
    show: '0',
    children: [
      {
        title: '0-0-0',
        key: '0-0-0',
        sbow: '0',
        children: [
          { title: '0-0-0-0', key: '0-0-0-0', show: '0' },
          { title: '0-0-0-1', key: '0-0-0-1', show: '0' },
          { title: '0-0-0-2', key: '0-0-0-2', show: '0' },
        ],
      },
      {
        title: '0-0-1',
        key: '0-0-1',
        show: '0',
        children: [
          { title: '0-0-1-0', key: '0-0-1-0', show: '0' },
          { title: '0-0-1-1', key: '0-0-1-1', show: '0' },
          { title: '0-0-1-2', key: '0-0-1-2', show: '0' },
        ],
      },
      {
        title: '0-0-2',
        key: '0-0-2',
        show: '0',
      },
    ],
  },
  {
    title: '0-1',
    key: '0-1',
    show: '0',
    children: [
      { title: '0-1-0-0', key: '0-1-0-0',show: '0' },
      { title: '0-1-0-1', key: '0-1-0-1',show: '0' },
      { title: '0-1-0-2', key: '0-1-0-2',show: '0' },
    ],
  },
  {
    title: '0-2',
    key: '0-2',
    show: '0',
  },
];


class Demo extends React.Component {
  state = {
    leftTreeData: [],
    rightTreeData: [],
   // prevChecked: [],
  };

  doChangeRight = (item) => {
    // eslint-disable-next-line react/no-access-state-in-setstate
    const data = this.state.rightTreeData;
    this.recursionChange(data, item);
    const temp = data;
    this.setState({ rightTreeData: temp });
  }

  
  recursionChange = (data, item) => {
    data.forEach(x => {
      if (x.children) {
        this.recursionChange(x.children, item);
        
      }
      if (x.key === item) {
        x.show = '1';
      }
      
    });
  }

  componentDidMount(){
    this.setState({leftTreeData: treeData});
    this.setState({rightTreeData: treeRightData});

    console.log(this.state.prevChecked);
    console.log(treeData);
    console.log(treeRightData);
  }

  onExpand = expandedKeys => {
    console.log('onExpand', expandedKeys);
    // if not set autoExpandParent to false, if children expanded, parent can not collapse.
    // or, you can remove all expanded children keys.
    this.setState({
      expandedKeys,
      autoExpandParent: false,
    });
  };

  doPrev(prev){
    prev.push("1212");
   console.log(prev);
  this.setState({prevChecked: prev});
  }

  onCheck = (checkedKeys, e) => {
    console.log('onCheck', checkedKeys);
    console.log('halfCheckedKeys', e.halfCheckedKeys);

    // console.log('this.state.prevCheckd',this.state.prevChecked);
    //  const prev = this.state.prevChecked;
    //  console.log('prev', prev);
     const addNodes = []; // 加集合
     const minusNodes = []; // 减集合
     const cur = []; // 当前所有要渲染的节点的key


   // // 获得当前选中所有节点，和原先节点比较决定加或减
   checkedKeys.forEach(item => {
     cur.push(item);
   });
   e.halfCheckedKeys.forEach(item => {
     cur.push(item);
   });

  //  if (prev.length === 0) {
  //    cur.forEach(item => {
  //      addNodes.push(item);
  //    });
  //  } else {
  //    for (let i = 0; i < cur.length; i++) {
  //      for (let j = 0; j < prev.length; j++) {
  //        if (cur[i] === prev[j]) {
  //          break;
  //        }
  //        if (j === (prev.length - 1)) {
  //          addNodes.push(cur[i]);
  //        }
  //      }
  //    }
  //  }

  //  if (cur.length === 0) {
  //    prev.forEach(item => {
  //      minusNodes.push(item);
  //    });
  //  } else {
  //    for (let i = 0; i < prev.length; i++) {
  //      for (let j = 0; j < cur.length; j++) {
  //        if (prev[i] === cur[j]) {
  //          break;
  //        }
  //        if (j === (cur.length - 1)) {
  //          minusNodes.push(prev[i]);
  //        }
        
  //      }
  //    }
  //  }
  //  for(let i = 0; i < cur.length; i++){
  //    prevChecked.push(cur[i]);
  //  }
  //  cur.forEach(item => {
  //    prevChecked.push(item);
  //  });
  // console.log('cur -> prev',prevChecked);

  //  this.doPrev(prev);
  //  console.log(prev);


  //  console.log('addNodes', addNodes);
  //  console.log('minusNodes', minusNodes);


   // addNodes.forEach(item => {
   //   this.doAddRight(item);
   // });
   //
   // minusNodes.forEach(item => {
   //   this.doMinusRight(item);
   // });

  //  this.setState({ prevChecked: cur });

  console.log('curData', cur);
  cur.forEach(item => {
    this.doChangeRight(item);
  });

  };

  onSelect = (selectedKeys, info) => {
    console.log('onSelect', info);
    this.setState({ selectedKeys });
  };
  click = () => {
    console.log
    prevChecked.push("1");

    console.log(prevChecked);
  }

  renderTreeNodes = data =>
    data.map(item => {
      if (item.children) {
        return (
          <TreeNode title={item.title} key={item.key} dataRef={item}>
            {this.renderTreeNodes(item.children)}
          </TreeNode>
        );
      }
      return <TreeNode key={item.key} {...item} />;
    });

    renderRightTreeNodes = data =>
    data.map(item => {
      if (item.children) {
        return (
          item.show === '1' &&
          <TreeNode title={item.title} key={item.key} dataRef={item}>
            {this.renderTreeNodes(item.children)}
          </TreeNode>
        );
      }
      return item.show === '1' && <TreeNode key={item.key} {...item} />;
    });

  render() {
    return (
      <div>
      <Tree
        checkable     
        onCheck={this.onCheck} 
      >
        {this.renderTreeNodes(this.state.leftTreeData)}
      </Tree>
      <Button onClick={this.click} >change</Button>
      <Divider type = "horizontal" />
      <Tree
        checkable     
       // checkedKeys={this.state.checkedKeys}    
      >
        {this.renderRightTreeNodes(this.state.rightTreeData)}
      </Tree>

      </div>
      
    );
  }
}

ReactDOM.render(<Demo />, document.getElementById('container'));
          
~~~

~~~jsx

import React from 'react';
import ReactDOM from 'react-dom';
import 'antd/dist/antd.css';
import './index.css';
import { Tree,Divider, Button } from 'antd';

const { TreeNode } = Tree;
const prevChecked = [];

// 左树
const treeData = [
  {
    title: '0-0',
    key: '0-0',
    children: [
      {
        title: '0-0-0',
        key: '0-0-0',
        children: [
          { title: '0-0-0-0', key: '0-0-0-0' },
          { title: '0-0-0-1', key: '0-0-0-1' },
          { title: '0-0-0-2', key: '0-0-0-2' },
        ],
      },
      {
        title: '0-0-1',
        key: '0-0-1',
        children: [
          { title: '0-0-1-0', key: '0-0-1-0' },
          { title: '0-0-1-1', key: '0-0-1-1' },
          { title: '0-0-1-2', key: '0-0-1-2' },
        ],
      },
      {
        title: '0-0-2',
        key: '0-0-2',
      },
    ],
  },
  {
    title: '0-1',
    key: '0-1',
    children: [
      { title: '0-1-0-0', key: '0-1-0-0' },
      { title: '0-1-0-1', key: '0-1-0-1' },
      { title: '0-1-0-2', key: '0-1-0-2' },
    ],
  },
  {
    title: '0-2',
    key: '0-2',
  },
];


// 右树
const treeRightData = [
  {
    title: '0-0',
    key: '0-0',
    show: '0',
    children: [
      {
        title: '0-0-0',
        key: '0-0-0',
        sbow: '0',
        children: [
          { title: '0-0-0-0', key: '0-0-0-0', show: '0' },
          { title: '0-0-0-1', key: '0-0-0-1', show: '0' },
          { title: '0-0-0-2', key: '0-0-0-2', show: '0' },
        ],
      },
      {
        title: '0-0-1',
        key: '0-0-1',
        show: '0',
        children: [
          { title: '0-0-1-0', key: '0-0-1-0', show: '0' },
          { title: '0-0-1-1', key: '0-0-1-1', show: '0' },
          { title: '0-0-1-2', key: '0-0-1-2', show: '0' },
        ],
      },
      {
        title: '0-0-2',
        key: '0-0-2',
        show: '0',
      },
    ],
  },
  {
    title: '0-1',
    key: '0-1',
    show: '0',
    children: [
      { title: '0-1-0-0', key: '0-1-0-0',show: '0' },
      { title: '0-1-0-1', key: '0-1-0-1',show: '0' },
      { title: '0-1-0-2', key: '0-1-0-2',show: '0' },
    ],
  },
  {
    title: '0-2',
    key: '0-2',
    show: '0',
  },
];


class Demo extends React.Component {
  state = {
    leftTreeData: [],
    rightTreeData: [],
   // prevChecked: [],
  };

  doChangeRight = (item) => {
    // eslint-disable-next-line react/no-access-state-in-setstate
    const data = this.state.rightTreeData;
    this.recursionChange(data, item);
    const temp = data;
    this.setState({ rightTreeData: temp });
  }

  
  recursionChange = (data, item) => {
    data.forEach(x => {
      if (x.children) {
        this.recursionChange(x.children, item);
        
      }
      if (x.key === item) {
        x.show = '1';
      }  
    });
  }

  componentDidMount(){
    this.setState({leftTreeData: treeData});
    this.setState({rightTreeData: treeRightData});

    console.log(this.state.prevChecked);
    console.log(treeData);
    console.log(treeRightData);
  }

  onExpand = expandedKeys => {
    console.log('onExpand', expandedKeys);
    // if not set autoExpandParent to false, if children expanded, parent can not collapse.
    // or, you can remove all expanded children keys.
    this.setState({
      expandedKeys,
      autoExpandParent: false,
    });
  };

  doPrev(prev){
    prev.push("1212");
   console.log(prev);
  this.setState({prevChecked: prev});
  }

  doClear = () => {
    const data = this.state.rightTreeData;
    data.forEach(x => {
      if (x.children) {
        this.recursionChange(x.children);
        
      }
        x.show = '0';
    });
    this.setState({rightTreeData: data});
  }

  onCheck = (checkedKeys, e) => {
    console.log('onCheck', checkedKeys);
    console.log('halfCheckedKeys', e.halfCheckedKeys);

    // console.log('this.state.prevCheckd',this.state.prevChecked);
    //  const prev = this.state.prevChecked;
    //  console.log('prev', prev);
    //  const addNodes = []; // 加集合
    //  const minusNodes = []; // 减集合
     const cur = []; // 当前所有要渲染的节点的key


   // // 获得当前选中所有节点，和原先节点比较决定加或减
   checkedKeys.forEach(item => {
     cur.push(item);
   });
   e.halfCheckedKeys.forEach(item => {
     cur.push(item);
   });

  //  if (prev.length === 0) {
  //    cur.forEach(item => {
  //      addNodes.push(item);
  //    });
  //  } else {
  //    for (let i = 0; i < cur.length; i++) {
  //      for (let j = 0; j < prev.length; j++) {
  //        if (cur[i] === prev[j]) {
  //          break;
  //        }
  //        if (j === (prev.length - 1)) {
  //          addNodes.push(cur[i]);
  //        }
  //      }
  //    }
  //  }

  //  if (cur.length === 0) {
  //    prev.forEach(item => {
  //      minusNodes.push(item);
  //    });
  //  } else {
  //    for (let i = 0; i < prev.length; i++) {
  //      for (let j = 0; j < cur.length; j++) {
  //        if (prev[i] === cur[j]) {
  //          break;
  //        }
  //        if (j === (cur.length - 1)) {
  //          minusNodes.push(prev[i]);
  //        }
        
  //      }
  //    }
  //  }
  //  for(let i = 0; i < cur.length; i++){
  //    prevChecked.push(cur[i]);
  //  }
  //  cur.forEach(item => {
  //    prevChecked.push(item);
  //  });
  // console.log('cur -> prev',prevChecked);

  //  this.doPrev(prev);
  //  console.log(prev);


  //  console.log('addNodes', addNodes);
  //  console.log('minusNodes', minusNodes);


   // addNodes.forEach(item => {
   //   this.doAddRight(item);
   // });
   //
   // minusNodes.forEach(item => {
   //   this.doMinusRight(item);
   // });

  //  this.setState({ prevChecked: cur });

  console.log('curData', cur);
  this.doClear();
  console.log('afterdoCLear',this.state.rightTreeData);
  cur.forEach(item => {
    this.doChangeRight(item);
  });

  };

  onSelect = (selectedKeys, info) => {
    console.log('onSelect', info);
    this.setState({ selectedKeys });
  };
  click = () => {
    console.log
    prevChecked.push("1");

    console.log(prevChecked);
  }

  renderTreeNodes = data =>
    data.map(item => {
      if (item.children) {
        return (
          <TreeNode title={item.title} key={item.key} dataRef={item}>
            {this.renderTreeNodes(item.children)}
          </TreeNode>
        );
      }
      return <TreeNode key={item.key} {...item} />;
    });

    renderRightTreeNodes = data =>
    data.map(item => {
      if (item.children) {
        return (
          item.show === '1' &&
          <TreeNode title={item.title} key={item.key} dataRef={item}>
            {this.renderTreeNodes(item.children)}
          </TreeNode>
        );
      }
      return item.show === '1' && <TreeNode key={item.key} {...item} />;
    });

  render() {
    return (
      <div>
      <Tree
        checkable     
        onCheck={this.onCheck} 
      >
        {this.renderTreeNodes(this.state.leftTreeData)}
      </Tree>
      <Button onClick={this.click} >change</Button>
      <Divider type = "horizontal" />
      <Tree
        checkable     
       // checkedKeys={this.state.checkedKeys}    
      >
        {this.renderRightTreeNodes(this.state.rightTreeData)}
      </Tree>

      </div>
      
    );
  }
}

ReactDOM.render(<Demo />, document.getElementById('container'));
          
~~~

~~~jsx
// 点击使隐藏或显示，并支持默认展开和选中
import React from 'react';
import ReactDOM from 'react-dom';
import 'antd/dist/antd.css';
import './index.css';
import { Tree,Divider, Button } from 'antd';

const { TreeNode } = Tree;
const prevChecked = [];

// 左树
const treeData = [
  {
    title: '0-0',
    key: '0-0',
    children: [
      {
        title: '0-0-0',
        key: '0-0-0',
        children: [
          { title: '0-0-0-0', key: '0-0-0-0' },
          { title: '0-0-0-1', key: '0-0-0-1' },
          { title: '0-0-0-2', key: '0-0-0-2' },
        ],
      },
      {
        title: '0-0-1',
        key: '0-0-1',
        children: [
          { title: '0-0-1-0', key: '0-0-1-0' },
          { title: '0-0-1-1', key: '0-0-1-1' },
          { title: '0-0-1-2', key: '0-0-1-2' },
        ],
      },
      {
        title: '0-0-2',
        key: '0-0-2',
      },
    ],
  },
  {
    title: '0-1',
    key: '0-1',
    children: [
      { title: '0-1-0-0', key: '0-1-0-0' },
      { title: '0-1-0-1', key: '0-1-0-1' },
      { title: '0-1-0-2', key: '0-1-0-2' },
    ],
  },
  {
    title: '0-2',
    key: '0-2',
  },
];


// 右树
const treeRightData = [
  {
    title: '0-0',
    key: '0-0',
    show: '0',
    children: [
      {
        title: '0-0-0',
        key: '0-0-0',
        sbow: '0',
        children: [
          { title: '0-0-0-0', key: '0-0-0-0', show: '0' },
          { title: '0-0-0-1', key: '0-0-0-1', show: '0' },
          { title: '0-0-0-2', key: '0-0-0-2', show: '0' },
        ],
      },
      {
        title: '0-0-1',
        key: '0-0-1',
        show: '0',
        children: [
          { title: '0-0-1-0', key: '0-0-1-0', show: '0' },
          { title: '0-0-1-1', key: '0-0-1-1', show: '0' },
          { title: '0-0-1-2', key: '0-0-1-2', show: '0' },
        ],
      },
      {
        title: '0-0-2',
        key: '0-0-2',
        show: '0',
      },
    ],
  },
  {
    title: '0-1',
    key: '0-1',
    show: '0',
    children: [
      { title: '0-1-0-0', key: '0-1-0-0',show: '0' },
      { title: '0-1-0-1', key: '0-1-0-1',show: '0' },
      { title: '0-1-0-2', key: '0-1-0-2',show: '0' },
    ],
  },
  {
    title: '0-2',
    key: '0-2',
    show: '0',
  },
];


class Demo extends React.Component {
  state = {
    leftTreeData: [],
    rightTreeData: [],
    checkedKeys: [],
    expandedKeys: [],
   // prevChecked: [],
  };

  doChangeRight = (item) => {
    // eslint-disable-next-line react/no-access-state-in-setstate
    const data = this.state.rightTreeData;
    this.recursionChange(data, item);
    const temp = data;
    this.setState({ rightTreeData: temp });
  }

  
  recursionChange = (data, item) => {
    data.forEach(x => {
      if (x.children) {
        this.recursionChange(x.children, item);
        
      }
      if (x.key === item) {
        x.show = '1';
      }  
    });
  }

  componentDidMount(){
    this.setState({leftTreeData: treeData});
    this.setState({rightTreeData: treeRightData});

    console.log(this.state.prevChecked);
    console.log(treeData);
    console.log(treeRightData);
  }

  onExpand = expandedKeys => {
    console.log('onExpand', expandedKeys);
    // if not set autoExpandParent to false, if children expanded, parent can not collapse.
    // or, you can remove all expanded children keys.
    this.setState({
      expandedKeys,
      autoExpandParent: false,
    });
  };

  doPrev(prev){
    prev.push("1212");
   console.log(prev);
  this.setState({prevChecked: prev});
  }

  doClear = () => {
    const data = this.state.rightTreeData;
    data.forEach(x => {
      if (x.children) {
        this.recursionChange(x.children);
        
      }
        x.show = '0';
    });
    this.setState({rightTreeData: data});
  }

  onCheck = (checkedKeys, e) => {
    console.log('onCheck', checkedKeys);
    console.log('halfCheckedKeys', e.halfCheckedKeys);

    // console.log('this.state.prevCheckd',this.state.prevChecked);
    //  const prev = this.state.prevChecked;
    //  console.log('prev', prev);
    //  const addNodes = []; // 加集合
    //  const minusNodes = []; // 减集合
     const cur = []; // 当前所有要渲染的节点的key


   // // 获得当前选中所有节点，和原先节点比较决定加或减
   checkedKeys.forEach(item => {
     cur.push(item);
   });
   e.halfCheckedKeys.forEach(item => {
     cur.push(item);
   });

  

  console.log('curData', cur);
  this.doClear();
  console.log('afterdoCLear',this.state.rightTreeData);
  cur.forEach(item => {
    this.doChangeRight(item);
  });
  this.setState({expandedKeys: cur});
  this.setState({checkedKeys: cur});

  };

  onSelect = (selectedKeys, info) => {
    console.log('onSelect', info);
    this.setState({ selectedKeys });
  };
  click = () => {
    console.log
    prevChecked.push("1");

    console.log(prevChecked);
  }

  renderTreeNodes = data =>
    data.map(item => {
      if (item.children) {
        return (
          <TreeNode title={item.title} key={item.key} dataRef={item}>
            {this.renderTreeNodes(item.children)}
          </TreeNode>
        );
      }
      return <TreeNode key={item.key} {...item} />;
    });

    renderRightTreeNodes = data =>
    data.map(item => {
      if (item.children) {
        return (
          item.show === '1' &&
          <TreeNode title={item.title} key={item.key} dataRef={item}>
            {this.renderTreeNodes(item.children)}
          </TreeNode>
        );
      }
      return item.show === '1' && <TreeNode key={item.key} {...item} />;
    });

  render() {
    return (
      <div>
      <Tree
        checkable     
        onCheck={this.onCheck} 
      >
        {this.renderTreeNodes(this.state.leftTreeData)}
      </Tree>
      <Button onClick={this.click} >change</Button>
      <Divider type = "horizontal" />
      <Tree
        checkable 
        checkedKeys={this.state.checkedKeys}
        expandedKeys={this.state.expandedKeys} 
       // checkedKeys={this.state.checkedKeys}    
      >
        {this.renderRightTreeNodes(this.state.rightTreeData)}
      </Tree>

      </div>
      
    );
  }
}

ReactDOM.render(<Demo />, document.getElementById('container'));
          
~~~

~~~jsx
如果只有expandKeys没有onExpand事件，则出现的情况是值能显示出来展开的Tree,但是点展开功能的时候无反应；autoExpandParent属性是:是否自动展开父节点,如果为true的话，关闭展开功能失效，所以要在onExpand的时候让这个属性变为false
下面我们就按照这个步骤走：


~~~

~~~jsx
// const temp = [
//   {
//     'category': 'z',
//     'categoryName': '客户信息附件',
//     'parentCategory': '0',
//     'parentCategoryName': '根',
//     'children': [
//       {
//         'category': 'z-1',
//         'categoryName': '其他附件',
//         'parentCategory': 'z',
//         'parentCategoryName': '客户信息附件',
//         'children': [
//           {
//             'category': 'z-1-1',
//             'categoryName': '身份证',
//             'parentCategory': 'z-1',
//             'parentCategoryName': '其他信息附件',
//             children: []
//           },
//           {
//             'category': 'z-1-2',
//             'categoryName': '户口簿',
//             'parentCategory': 'z-1',
//             'parentCategoryName': '其他信息附件',
//             children: []
//           }
//         ]
//       },
//       {
//         'category': 'z-2',
//         'categoryName': '重要附件',
//         'parentCategory': 'z',
//         'parentCategoryName': '客户信息附件',
//         'children': [
//           {
//             'category': 'z-2-1',
//             'categoryName': '合同照片',
//             'parentCategory': 'z-2',
//             'parentCategoryName': '重要附件',
//             children: [],
//
//           }
//         ]
//       }
//     ]
//   }
// ];


// const leftTreeData = temp;
    // this.setState({ leftTreeData });
    // this.doSelectAllKeys(leftTreeData);
    // const tempData = leftTreeData;
    // this.doRecursion(tempData);
    // this.setState({ rightTreeData: tempData });
~~~

